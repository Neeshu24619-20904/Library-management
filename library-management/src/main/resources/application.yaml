spring:
  datasource:
    url: jdbc:mysql://localhost:3306/library_db
    username: root
    password: Moonwalk@24619
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp

server:
  port: 8080